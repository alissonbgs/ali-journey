#name: CI
#
#on:
#  pull_request:
#
#jobs:
#  build:
#    runs-on: ubuntu-latest
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v4
#      - name: Setup Node
#        uses: actions/setup-node@v4
#        with:
#          node-version: 20
#          cache: npm
#          cache-dependency-path: package-lock.json
#      - name: Install
#        run: npm ci --include=optional
#      - name: Ensure Tailwind native binary
#        if: runner.os == 'Linux'
#        run: npm install --workspace web --no-save --no-package-lock @tailwindcss/oxide-linux-x64-gnu
#      - name: Typecheck
#        run: npm run typecheck:ui
#      - name: Test
#        run: npm run test:ui
#      - name: Build
#        run: npm run build:ui
